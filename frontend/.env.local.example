# Copy to .env.local
#
# ONLY the API base belongs here. Anything prefixed NEXT_PUBLIC_ is inlined
# into the JavaScript bundle and shipped to every visitor -- never put the FRED
# API key in this file. The key lives in backend/.env and never leaves the
# server.
NEXT_PUBLIC_API_BASE=http://localhost:8000
